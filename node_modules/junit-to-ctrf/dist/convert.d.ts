import type { Report } from 'ctrf';
import type { JUnitTestCase } from '../types/junit.js';
/**
 * Options for the conversion
 */
export interface ConvertOptions {
    outputPath?: string;
    toolName?: string;
    envProps?: string[];
    useSuiteName?: boolean;
    log?: boolean;
}
/**
 * Convert JUnit XML report(s) to CTRF
 * @param pattern - Path to JUnit XML file or glob pattern
 * @param options - Optional options for the conversion
 * @returns Promise that resolves when the conversion is complete
 */
export declare function convertJUnitToCTRFReport(pattern: string, options?: ConvertOptions): Promise<Report | null>;
/**
 * Sanitize a string to ensure it's valid for JSON serialization
 * Removes or escapes problematic characters that could break JSON parsing
 * @param str - String to sanitize
 * @returns Sanitized string safe for JSON
 */
export declare function sanitizeString(str?: string): string | undefined;
export declare function createCTRFReport(testCases: JUnitTestCase[], toolName?: string, envProps?: Record<string, string>, useSuiteName?: boolean): Report;
