"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.sendAISummaryToTeams = exports.sendFlakyResultsToTeams = exports.sendTestResultsToTeams = void 0;
const message_formatter_1 = require("./message-formatter");
const teams_notify_1 = require("./teams-notify");
function sendTestResultsToTeams(report_1) {
    return __awaiter(this, arguments, void 0, function* (report, options = {}, logs = false) {
        if (options.token) {
            process.env.TEAMS_WEBHOOK_URL = options.token;
        }
        if (options.onFailOnly && report.results.summary.failed === 0) {
            logs && console.log('No failed tests. Message not sent.');
            return;
        }
        const message = (0, message_formatter_1.formatResultsMessage)(report);
        yield (0, teams_notify_1.sendTeamsMessage)(message);
        logs && console.log('Test results message sent to Teams.');
    });
}
exports.sendTestResultsToTeams = sendTestResultsToTeams;
function sendFlakyResultsToTeams(report_1) {
    return __awaiter(this, arguments, void 0, function* (report, options = {}, logs = false) {
        if (options.token) {
            process.env.TEAMS_WEBHOOK_URL = options.token;
        }
        const message = (0, message_formatter_1.formatFlakyTestsMessage)(report);
        if (message) {
            yield (0, teams_notify_1.sendTeamsMessage)(message);
            logs && console.log('Flaky tests message sent to Teams.');
        }
        else {
            logs && console.log('No flaky tests detected. No message sent.');
        }
    });
}
exports.sendFlakyResultsToTeams = sendFlakyResultsToTeams;
function sendAISummaryToTeams(report_1) {
    return __awaiter(this, arguments, void 0, function* (report, options = {}, logs = false) {
        if (options.token) {
            process.env.TEAMS_WEBHOOK_URL = options.token;
        }
        for (const test of report.results.tests) {
            if (test.status === "failed") {
                const message = (0, message_formatter_1.formatAiSummaryForTest)(test, report.results.environment || {});
                if (message) {
                    yield (0, teams_notify_1.sendTeamsMessage)(message);
                    logs && console.log(`AI summary message sent to Teams for ${test.name}.`);
                }
            }
        }
    });
}
exports.sendAISummaryToTeams = sendAISummaryToTeams;
