#!/usr/bin/env node
"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const yargs_1 = __importDefault(require("yargs/yargs"));
const helpers_1 = require("yargs/helpers");
const ctrf_parser_1 = require("./ctrf-parser");
const message_formatter_1 = require("./message-formatter");
const teams_notify_1 = require("./teams-notify");
const argv = (0, yargs_1.default)((0, helpers_1.hideBin)(process.argv))
    .command('results <path>', 'Send test results summary to Teams', (yargs) => {
    return yargs
        .positional('path', {
        describe: 'Path to the CTRF file',
        type: 'string',
        demandOption: true,
    })
        .option('onFailOnly', {
        alias: 'f',
        type: 'boolean',
        description: 'Send message only if there are failed tests',
        default: false,
    });
}, (argv) => __awaiter(void 0, void 0, void 0, function* () {
    try {
        const ctrfData = (0, ctrf_parser_1.parseCtrfFile)(argv.path);
        if (argv.onFailOnly && ctrfData.results.summary.failed === 0) {
            console.log('No failed tests. Message not sent.');
            return;
        }
        const message = (0, message_formatter_1.formatResultsMessage)(ctrfData);
        yield (0, teams_notify_1.sendTeamsMessage)(message);
        console.log('Results message sent to Teams.');
    }
    catch (error) {
        console.error('Error:', error.message);
    }
}))
    .command('fail-details <path>', 'Send failed test results to Teams', (yargs) => {
    return yargs.positional('path', {
        describe: 'Path to the CTRF file',
        type: 'string',
        demandOption: true,
    });
}, (argv) => __awaiter(void 0, void 0, void 0, function* () {
    try {
        const ctrfData = (0, ctrf_parser_1.parseCtrfFile)(argv.path);
        const message = (0, message_formatter_1.formatFailedTestsMessage)(ctrfData);
        // await sendTeamsMessage(message);
        console.log('Coming soon!');
    }
    catch (error) {
        console.error('Error:', error.message);
    }
}))
    .command('flaky <path>', 'Send flaky test results to Teams', (yargs) => {
    return yargs.positional('path', {
        describe: 'Path to the CTRF file',
        type: 'string',
        demandOption: true,
    });
}, (argv) => __awaiter(void 0, void 0, void 0, function* () {
    try {
        const ctrfData = (0, ctrf_parser_1.parseCtrfFile)(argv.path);
        const message = (0, message_formatter_1.formatFlakyTestsMessage)(ctrfData);
        if (message) {
            yield (0, teams_notify_1.sendTeamsMessage)(message);
            console.log('Flaky tests message sent to Teams.');
        }
        else {
            console.log('No flaky tests detected. No message sent.');
        }
    }
    catch (error) {
        console.error('Error:', error.message);
    }
}))
    .command('ai <path>', 'Send ai failure test summary to Teams', (yargs) => {
    return yargs.positional('path', {
        describe: 'Path to the CTRF file',
        type: 'string',
        demandOption: true,
    });
}, (argv) => __awaiter(void 0, void 0, void 0, function* () {
    try {
        const ctrfData = (0, ctrf_parser_1.parseCtrfFile)(argv.path);
        for (const test of ctrfData.results.tests) {
            if (test.status === "failed") {
                const message = (0, message_formatter_1.formatAiSummaryForTest)(test, ctrfData.results.environment || {});
                if (message) {
                    yield (0, teams_notify_1.sendTeamsMessage)(message);
                    console.log(`AI summary message sent to Teams for ${test.name}.`);
                }
            }
        }
    }
    catch (error) {
        console.error('Error:', error.message);
    }
}))
    .help()
    .argv;
