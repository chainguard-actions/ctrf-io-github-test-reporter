"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getTestName = exports.stripAnsiFromErrors = exports.stripAnsi = exports.ansiRegex = void 0;
function ansiRegex({ onlyFirst = false } = {}) {
    const pattern = [
        '[\\u001B\\u009B][[\\]()#;?]*(?:(?:(?:(?:;[-a-zA-Z\\d\\/#&.:=?%@~_]+)*|[a-zA-Z\\d]+(?:;[-a-zA-Z\\d\\/#&.:=?%@~_]*)*)?\\u0007)',
        '(?:(?:\\d{1,4}(?:;\\d{0,4})*)?[\\dA-PR-TZcf-ntqry=><~]))',
    ].join('|');
    return new RegExp(pattern, onlyFirst ? undefined : 'g');
}
exports.ansiRegex = ansiRegex;
function stripAnsi(message) {
    if (typeof message !== 'string') {
        throw new TypeError(`Expected a \`string\`, got \`${typeof message}\``);
    }
    let sanitizedMessage = message.replace(ansiRegex(), '');
    sanitizedMessage = sanitizedMessage.replace(/[^\x00-\x7F]/g, '');
    return sanitizedMessage;
}
exports.stripAnsi = stripAnsi;
function stripAnsiFromErrors(report) {
    var _a;
    if (!((_a = report === null || report === void 0 ? void 0 : report.results) === null || _a === void 0 ? void 0 : _a.tests)) {
        return report;
    }
    report.results.tests.forEach((test) => {
        if (test.message) {
            test.message = stripAnsi(test.message);
        }
        if (test.trace) {
            test.trace = stripAnsi(test.trace);
        }
    });
    return report;
}
exports.stripAnsiFromErrors = stripAnsiFromErrors;
function getTestName(test, useSuiteName) {
    if (useSuiteName && test.suite) {
        return `${test.suite}:${test.name}`;
    }
    return test.name;
}
exports.getTestName = getTestName;
