#!/usr/bin/env node
"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const yargs_1 = __importDefault(require("yargs/yargs"));
const helpers_1 = require("yargs/helpers");
const ctrf_parser_1 = require("./ctrf-parser");
const slack_reporter_1 = require("./slack-reporter");
const sharedOptions = {
    title: {
        alias: 't',
        type: 'string',
        description: 'Title of notification',
    },
    prefix: {
        alias: 'p',
        type: 'string',
        description: 'Custom text to add as a prefix to the message',
        default: '',
    },
    suffix: {
        alias: 's',
        type: 'string',
        description: 'Custom text to add as a suffix to the message',
        default: '',
    },
};
const consolidatedOption = {
    consolidated: {
        alias: 'c',
        type: 'boolean',
        description: 'Consolidate all failure summaries into a single message',
        default: false,
    },
};
const argv = (0, yargs_1.default)((0, helpers_1.hideBin)(process.argv))
    .command('results <path>', 'Send test results summary to Slack', (yargs) => {
    return yargs
        .positional('path', {
        describe: 'Path to the CTRF file',
        type: 'string',
        demandOption: true,
    })
        .option('onFailOnly', {
        alias: 'f',
        type: 'boolean',
        description: 'Send message only if there are failed tests',
        default: false,
    })
        .options(sharedOptions);
}, (argv) => __awaiter(void 0, void 0, void 0, function* () {
    try {
        const report = (0, ctrf_parser_1.parseCtrfFile)(argv.path);
        yield (0, slack_reporter_1.sendTestResultsToSlack)(report, { title: argv.title, prefix: argv.prefix, suffix: argv.suffix, onFailOnly: argv.onFailOnly }, true);
    }
    catch (error) {
        console.error('Error:', error.message);
    }
}))
    .command('failed <path>', 'Send failed test results to Slack', (yargs) => {
    return yargs.positional('path', {
        describe: 'Path to the CTRF file',
        type: 'string',
        demandOption: true,
    })
        .options(sharedOptions)
        .option(consolidatedOption);
}, (argv) => __awaiter(void 0, void 0, void 0, function* () {
    try {
        const report = (0, ctrf_parser_1.parseCtrfFile)(argv.path);
        yield (0, slack_reporter_1.sendFailedResultsToSlack)(report, { title: argv.title, prefix: argv.prefix, suffix: argv.suffix, consolidated: argv.consolidated }, true);
    }
    catch (error) {
        console.error('Error:', error.message);
    }
}))
    .command('flaky <path>', 'Send flaky test results to Slack', (yargs) => {
    return yargs.positional('path', {
        describe: 'Path to the CTRF file',
        type: 'string',
        demandOption: true,
    })
        .options(sharedOptions);
}, (argv) => __awaiter(void 0, void 0, void 0, function* () {
    try {
        const report = (0, ctrf_parser_1.parseCtrfFile)(argv.path);
        yield (0, slack_reporter_1.sendFlakyResultsToSlack)(report, { title: argv.title, prefix: argv.prefix, suffix: argv.suffix }, true);
    }
    catch (error) {
        console.error('Error:', error.message);
    }
}))
    .command('ai <path>', 'Send ai failed test summary for each failed test to Slack', (yargs) => {
    return yargs.positional('path', {
        describe: 'Path to the CTRF file',
        type: 'string',
        demandOption: true,
    })
        .options(sharedOptions)
        .option(consolidatedOption);
}, (argv) => __awaiter(void 0, void 0, void 0, function* () {
    try {
        const report = (0, ctrf_parser_1.parseCtrfFile)(argv.path);
        yield (0, slack_reporter_1.sendAISummaryToSlack)(report, { title: argv.title, prefix: argv.prefix, suffix: argv.suffix, consolidated: argv.consolidated }, true);
    }
    catch (error) {
        console.error('Error:', error.message);
    }
}))
    .help()
    .argv;
