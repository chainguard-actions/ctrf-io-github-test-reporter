"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseCtrfFile = void 0;
const common_1 = require("./utils/common");
const ctrf_1 = require("ctrf");
function parseCtrfFile(pattern) {
    console.log(`Reading CTRF reports from ${pattern}`);
    const reports = (0, ctrf_1.readReportsFromGlobPattern)(pattern);
    if (reports.length === 0) {
        throw new Error(`CTRF report not found at: ${pattern}`);
    }
    let report = reports.length > 1 ? (0, ctrf_1.mergeReports)(reports) : reports[0];
    report = (0, common_1.stripAnsiFromErrors)(report);
    console.log(`Read ${reports.length} CTRF reports`);
    return report;
}
exports.parseCtrfFile = parseCtrfFile;
