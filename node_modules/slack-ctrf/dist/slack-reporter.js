"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.sendAISummaryToSlack = exports.sendFlakyResultsToSlack = exports.sendFailedResultsToSlack = exports.sendTestResultsToSlack = void 0;
const message_formatter_1 = require("./message-formatter");
const slack_notify_1 = require("./slack-notify");
const common_1 = require("./utils/common");
function sendTestResultsToSlack(report_1) {
    return __awaiter(this, arguments, void 0, function* (report, options = {}, logs = false) {
        var _a, _b, _c;
        if (options.token) {
            process.env.SLACK_WEBHOOK_URL = options.token;
        }
        if (options.onFailOnly && report.results.summary.failed === 0) {
            logs && console.log('No failed tests. Message not sent.');
            return;
        }
        const message = (0, message_formatter_1.formatResultsMessage)(report, Object.assign(Object.assign({}, options), { title: (_a = options.title) !== null && _a !== void 0 ? _a : "Test Results", prefix: (_b = options.prefix) !== null && _b !== void 0 ? _b : "", suffix: (_c = options.suffix) !== null && _c !== void 0 ? _c : "" }));
        yield (0, slack_notify_1.sendSlackMessage)(message);
        logs && console.log('Test results message sent to Slack.');
    });
}
exports.sendTestResultsToSlack = sendTestResultsToSlack;
function sendFailedResultsToSlack(report_1) {
    return __awaiter(this, arguments, void 0, function* (report, options = {}, logs = false) {
        var _a, _b, _c;
        if (options.token) {
            process.env.SLACK_WEBHOOK_URL = options.token;
        }
        if (report.results.summary.failed === 0) {
            return;
        }
        report = (0, common_1.stripAnsiFromErrors)(report);
        if (options.consolidated) {
            const message = (0, message_formatter_1.formatConsolidatedFailedTestSummary)(report.results.tests, report.results.environment, Object.assign(Object.assign({}, options), { title: (_a = options.title) !== null && _a !== void 0 ? _a : "Failed Test Results", prefix: (_b = options.prefix) !== null && _b !== void 0 ? _b : "", suffix: (_c = options.suffix) !== null && _c !== void 0 ? _c : "" }));
            if (message) {
                yield (0, slack_notify_1.sendSlackMessage)(message);
                logs && console.log('Failed test summary sent to Slack.');
            }
            else {
                logs && console.log('No failed test summary detected. No message sent.');
            }
        }
        else {
            for (const test of report.results.tests) {
                if (test.status === "failed") {
                    const message = (0, message_formatter_1.formatFailedTestSummary)(test, report.results.environment, options);
                    if (message) {
                        yield (0, slack_notify_1.sendSlackMessage)(message);
                        logs && console.log('Failed test summary sent to Slack.');
                    }
                    else {
                        logs && console.log('No failed test summary detected. No message sent');
                    }
                }
            }
        }
    });
}
exports.sendFailedResultsToSlack = sendFailedResultsToSlack;
function sendFlakyResultsToSlack(report_1) {
    return __awaiter(this, arguments, void 0, function* (report, options = {}, logs = false) {
        var _a, _b, _c;
        if (options.token) {
            process.env.SLACK_WEBHOOK_URL = options.token;
        }
        const message = (0, message_formatter_1.formatFlakyTestsMessage)(report, Object.assign(Object.assign({}, options), { title: (_a = options.title) !== null && _a !== void 0 ? _a : "Flaky Tests", prefix: (_b = options.prefix) !== null && _b !== void 0 ? _b : "", suffix: (_c = options.suffix) !== null && _c !== void 0 ? _c : "" }));
        if (message) {
            yield (0, slack_notify_1.sendSlackMessage)(message);
            logs && console.log('Flaky tests message sent to Slack.');
        }
        else {
            logs && console.log('No flaky tests detected. No message sent.');
        }
    });
}
exports.sendFlakyResultsToSlack = sendFlakyResultsToSlack;
function sendAISummaryToSlack(report_1) {
    return __awaiter(this, arguments, void 0, function* (report, options = {}, logs = false) {
        var _a, _b, _c;
        if (options.token) {
            process.env.SLACK_WEBHOOK_URL = options.token;
        }
        if (options.consolidated) {
            const message = (0, message_formatter_1.formatConsolidatedAiTestSummary)(report.results.tests, report.results.environment, Object.assign(Object.assign({}, options), { title: (_a = options.title) !== null && _a !== void 0 ? _a : "AI Test Summary", prefix: (_b = options.prefix) !== null && _b !== void 0 ? _b : "", suffix: (_c = options.suffix) !== null && _c !== void 0 ? _c : "" }));
            if (message) {
                yield (0, slack_notify_1.sendSlackMessage)(message);
                logs && console.log('AI test summary sent to Slack.');
            }
            else {
                logs && console.log('No AI summary detected. No message sent.');
            }
        }
        else {
            for (const test of report.results.tests) {
                if (test.status === "failed") {
                    const message = (0, message_formatter_1.formatAiTestSummary)(test, report.results.environment, options);
                    if (message) {
                        yield (0, slack_notify_1.sendSlackMessage)(message);
                        logs && console.log('AI test summary sent to Slack.');
                    }
                    else {
                        logs && console.log('No AI summary detected. No message sent');
                    }
                }
            }
        }
    });
}
exports.sendAISummaryToSlack = sendAISummaryToSlack;
